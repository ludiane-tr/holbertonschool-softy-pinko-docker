# Use the latest version of Ubuntu
FROM ubuntu:latest

# Update package list
RUN apt-get update

# Upgrade installed packages
RUN apt-get upgrade -y

# Use JSON array syntax to avoid signal issues
CMD ["echo", "Hello, World!"]
